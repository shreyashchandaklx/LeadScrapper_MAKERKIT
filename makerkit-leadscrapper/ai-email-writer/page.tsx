import LeadscrapperFrame from '../_components/LeadscrapperFrame';

export const metadata = { title: 'Leadscrapper — AI Email Writer' };

export default function AiEmailWriterPage() {
  return <LeadscrapperFrame page="email-gen" title="AI Email Writer" />;
}
