import LeadscrapperFrame from '../_components/LeadscrapperFrame';

export const metadata = { title: 'Leadscrapper — Lead Manager' };

export default function LeadManagerPage() {
  return <LeadscrapperFrame page="leads" title="Lead Manager" />;
}
